server:
#  servlet:
#    context-path: /security-session
  port: 8080
spring:
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org:
      springframework:
        security: trace