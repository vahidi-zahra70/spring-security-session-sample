server:
  servlet:
    session:
      timeout: 10m
#      cookie:
#        name: PracticeSession
  #  servlet:
#    context-path: /security-session
  port: 8080
spring:
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org:
      springframework:
        security: debug
        session: debug