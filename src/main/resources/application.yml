server:
  servlet:
    session:
      timeout: 10m
      cookie:
        max-age: 10m
        name: session-test
#    servlet:
#    context-path: /security-session
  port: 8087
spring:
  session:
    redis:
      namespace: mvc-sample:sessions
      repository-type: indexed
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org:
      springframework:
        security: debug
        session: debug
